# -----------------------------------------------------------------------------
# -- DATABASE & PRISMA
# -----------------------------------------------------------------------------
# The connection string for your database.
# For Vercel Postgres, this will be provided in your project dashboard.
# For local development, you can use: "postgresql://user:password@localhost:5432/mydb"
DATABASE_URL="postgresql://user:password@host:port/database"


# -----------------------------------------------------------------------------
# -- SECURITY & AUTHENTICATION
# -----------------------------------------------------------------------------
# A 32-byte (64-character hex) secret key for AES-256 encryption.
# Generate a secure key with: openssl rand -hex 32
ENCRYPTION_KEY="your-64-character-aes-256-encryption-key-here"

# A secure, random string used to protect the admin API endpoints.
# Generate a secure key with: openssl rand -hex 32
ADMIN_API_KEY="your-secure-admin-api-key-here"


# -----------------------------------------------------------------------------
# -- PAYMENT (Sepay)
# -----------------------------------------------------------------------------
# Your API key for authenticating with the Sepay service.
SEPAY_API_KEY="your-sepay-api-key-here"
# The base URL for the Sepay API.
SEPAY_API_URL="https://api.sepay.vn"


# -----------------------------------------------------------------------------
# -- NOTIFICATIONS
# -----------------------------------------------------------------------------
# --- Telegram (Primary Channel) ---
# The token for your Telegram Bot, obtained from BotFather.
TELEGRAM_BOT_TOKEN="your-telegram-bot-token-here"
# The unique identifier for your private channel or group chat.
TELEGRAM_CHAT_ID="your-telegram-chat-id-here"

# --- Resend (Backup Email Channel) ---
# Your API key for the Resend email service.
RESEND_API_KEY="your-resend-api-key-here"
# The email address that notifications will be sent from.
EMAIL_FROM_ADDRESS="noreply@yourdomain.com"
# The email address where backup notifications will be sent.
OWNER_EMAIL_ADDRESS="owner@yourdomain.com"

# -----------------------------------------------------------------------------
# -- APPLICATION
# -----------------------------------------------------------------------------
# The base URL of your deployed application. Required for webhooks and links.
NEXT_PUBLIC_APP_URL="http://localhost:3000"